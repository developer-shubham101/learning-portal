services:
  mongodb:
    image: mongo:8
    container_name: learning-portal-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - learning_portal_mongo:/data/db

volumes:
  learning_portal_mongo:
